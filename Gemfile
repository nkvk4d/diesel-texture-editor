# Gemfile
source "https://rubygems.org"

# Основные зависимости
gem "gtk3"           # GUI фреймворк (GTK+ 3 bindings for Ruby)
gem "rubyzip"        # Создание/чтение ZIP архивов (для .jar файлов)

# Примечание: gem 'cairo' обычно автоматически устанавливается как зависимость gtk3,
# но если потребуется отдельно - раскомментируйте:
# gem "cairo"

group :development do
  # gem "pry"        # отладка (опционально)
  # gem "rubocop"    # линтер (опционально)
end
